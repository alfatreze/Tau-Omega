<script lang="ts">
  import type { DuplicateGroup } from './types';
  export let path = ''; export let groups: DuplicateGroup[] | null = null; export let notice = ''; export let loading = false;
  export let choose: () => void; export let scan: () => void;
</script>
<section class="jobs-panel page" aria-labelledby="problems-title"><header><div><p class="eyebrow">LIBRARY HEALTH</p><h1 id="problems-title">Problems</h1><p class="lede">Find duplicate media safely. Nothing is changed or deleted.</p></div></header><section class="settings-card"><div class="picker-row"><input bind:value={path} placeholder="/Users/me/Music"/><button class="picker" on:click={choose}>Choose</button><button class="primary" disabled={loading} on:click={scan}>{loading ? 'Scanning…' : 'Find duplicates'}</button></div><p class="notice" role="status">{notice}</p></section>{#if groups?.length}<section class="settings-card"><h2>{groups.length} duplicate groups</h2>{#each groups as group}<div class="settings-values"><div><span>{group.files.length} matching files</span><strong>{group.files.join(' · ')}</strong></div></div>{/each}</section>{:else if groups}<section class="empty"><div class="empty-art">✓</div><h2>No duplicates found</h2><p>This media root has no matching file hashes.</p></section>{:else}<section class="empty"><div class="empty-art">!</div><h2>No folder scanned</h2><p>Choose a local media root to inspect it.</p></section>{/if}</section>
