<script lang="ts">
  import type { LibrarySummary } from './types';
  export let path = ''; export let summary: LibrarySummary | null = null; export let notice = ''; export let choose: () => void; export let scan: () => void;
</script>
<section class="jobs-panel page" aria-labelledby="library-title"><header><div><p class="eyebrow">LIBRARY</p><h1 id="library-title">Library overview</h1><p class="lede">A read-only summary of the media Tau Omega can index.</p></div></header><section class="settings-card"><div class="picker-row"><input bind:value={path} placeholder="/Users/me/Music"/><button class="picker" on:click={choose}>Choose</button><button class="primary" on:click={scan}>Inspect</button></div><p class="notice" role="status">{notice}</p></section>{#if summary}<section class="comparison-counts"><span><b>{summary.tracks}</b> tracks</span><span><b>{summary.playlists}</b> playlists</span></section>{#if summary.warnings.length}<section class="settings-card"><h2>Library warnings</h2><ul>{#each summary.warnings as warning}<li>{warning.message}</li>{/each}</ul></section>{/if}{:else}<section class="empty"><div class="empty-art">τ</div><h2>No library selected</h2><p>Choose a local media root to inspect it.</p></section>{/if}</section>
