[package]
name = "tau-omega"
version = "0.1.0"
edition = "2024"

[workspace]

[dependencies]
tau-core = { path = "../crates/tau-core" }
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[build-dependencies]
tauri-build = { version = "2", features = [] }
